# AI Agent 프로젝트 Peer Review Template (PRT)

- **코더** : elfinnani-byte — `khu-academic-agent` (대학교 학사 안내 고객응대 에이전트)
- **리뷰어** : mjkimlunar — `ismsp-agent`

---

## PRT (Peer Review Template)

### 1. 실행 가능성 (Executability)

*에이전트가 실제로 돌아가는가*

- [O] 코더가 남긴 실행 결과(로그·캡처·데모)를 확인했다
- [O] 직접 clone 해서 실행해봤다 → **구조·데이터 확인까지 성공** (LLM 호출은 본인 키로 돌리지 않음)
- 막혔다면 어디서: **없음.** `README.md` 의 빠른 시작 3줄이 그대로 따라 하면 되는 형태였고,
  `python evaluate.py` 로 지표만 따로 볼 수 있게 갈라 둔 점이 특히 좋았습니다.

**근거 1 — 실행 결과가 세 갈래로 남아 있음**

```
screenshots/benchmark.png       성능 벤치마크 화면
screenshots/chat_demo.png       상담 데모 화면
screenshots/experiment_log.png  개선 기록 화면
experiment_log.csv              회차별 수치 원본
```

**근거 2 — 데이터가 실제로 들어 있음** (문서만 있고 데이터가 비는 경우가 많은데 그렇지 않았습니다)

```
data/routing_answers.csv     101줄 (라우팅 평가셋 100건)
data/student_inquiries.csv   101줄
data/hard_cases.csv           31줄 (함정 사례 30건)
guide_docs/                   규정 원문 6종 (md + 원본 pdf 동봉)
```

**한 줄 코멘트**: 원본 PDF 와 변환한 md 를 **둘 다** 넣어 둔 것이 인상 깊었습니다.
근거 문서의 출처를 나중에 검증할 수 있게 하는 선택이라고 읽었습니다.

---

### 2. 아키텍처 가독성 (Architecture Legibility)

*에이전트의 제어 흐름이 코드에서 드러나는가*

- [O] 그래프 그림이나 구조 설명이 있다 → `REPORT.md` §5 에 Mermaid 구조도
- [O] 노드 이름만 봐도 무슨 일을 하는지 짐작된다
- [O] **왜 이 패턴을 골랐는지** 한 줄이라도 적혀 있다

**근거 1 — 노드 이름이 파이프라인 단계와 1:1**

```python
# agent.py
g.add_node("route", node_route)        # 분류
g.add_node("answer", node_answer)      # 근거 조회 + 답변
g.add_node("guard", node_guard)        # 숫자 검증
g.add_node("escalate", node_escalate)  # 이관
g.add_conditional_edges("route",  after_route,  {"answer": "answer", "escalate": "escalate"})
g.add_conditional_edges("answer", after_answer, {"guard": "guard", END: END})
```

**근거 2 — 구조도에 실패 경로까지 그려져 있음**

구조도가 정상 경로만 그려진 경우가 많은데, 여기는 **이관이 세 군데서 갈라지는 것**이
그림에 다 나옵니다. `conf < 0.5` → ESCALATE, `route == OTHER` → OUT_OF_SCOPE,
가드레일 재시도 소진 → ESCALATE. 그림만 보고도 "이 에이전트는 어떤 때 답을 안 하는가" 를
알 수 있었습니다.

**근거 3 — 왜 그렇게 했는지가 적혀 있음** (§5 아래 네 줄)

> **멀티턴**: `InMemorySaver` 체크포인터로 `thread_id`별 대화 상태를 유지하되, 각 턴의
> 라우팅·답변 생성은 직전 턴의 질문을 이어붙이지 않고 현재 질문만으로 독립 판단합니다 —
> 대화 맥락과 무관하게 매 턴을 독립적으로 분류·답변해 이전 주제가 새 질문에 섞이지 않습니다.

**가장 궁금했던 노드 / 부분**: `node_guard` 와 위 멀티턴 결정입니다.
저는 같은 문제에서 **정반대 선택**(이전 문의·답변을 라우터에 넘김)을 했는데,
그래서 "그건 언제까지예요?" 같은 되묻기는 풀리지만 새 주제에 앞 주제가 섞이는 사고를 겪었습니다.
코더님은 그 사고를 아예 구조로 막은 대신 되묻기를 포기한 것으로 보여서,
어느 쪽이 학사 상담에 맞는지가 가장 궁금했습니다. (→ 5번 질문)

---

### 3. 측정 가능성 (Measurability)

*성능을 숫자로 말할 수 있는가*

- [O] 성능을 나타내는 숫자가 하나라도 있다
- [O] 그 숫자를 **어떻게 쟀는지**(평가셋, 기준) 적혀 있다
- [O] 개선 **전 → 후** 비교가 있다

**근거 1 — 지표마다 평가셋 크기가 같이 적혀 있음** (§4.1)

| 지표 | 값 | 평가셋 |
| --- | --- | --- |
| 라우팅 정확도 | 0.87~0.90 | eval 60건 |
| 라우팅 macro F1 | 0.90~0.92 | 4개 카테고리 |
| 범위밖 인식률 | 0.550 | outscope 20건 |
| 1턴 답변 통과율 | 100% (16/16) | 골든셋 16건 |

**근거 2 — 수치의 흔들림을 숨기지 않음**

> 라우팅 정확도·macro F1은 `evaluate.py`를 반복 실행할 때마다 소수점 둘째 자리 수준으로
> 변동합니다(`temperature=0`으로 호출하지만 …). 범위밖 인식률과 답변 통과율은 여러 차례
> 재실행해도 각각 0.550, 100%로 고정적으로 재현되었습니다.

**어느 지표가 흔들리고 어느 지표가 고정인지를 갈라 적은 것**이 이 리포트에서 가장 좋았던
부분입니다. 저도 같은 문제를 겪었는데 저는 "±9%p 흔들린다" 정도로만 적었고,
코더님처럼 지표별로 나눠 보지는 못했습니다.

**근거 3 — 실패를 수치가 아니라 사례로 파고듦** (§4.3)

범위밖 인식률이 0.6 → 0.55 로 **떨어진 것**을 그대로 적고, 오분류 8건을 표로 나열한 뒤
원인을 짚었습니다.

> 회차 2에서 추가한 … confidence 상승 규칙이 OTHER 트랩 문항에도 그대로 적용되어,
> "기숙사/장학금/등록금/편입/자퇴/제적" 같은 표면 키워드가 하나라도 잡히면 모델이
> 고확신으로 해당 카테고리에 배정해버리는 부작용이 발생했습니다.

지표가 내려간 회차를 지우지 않고 남긴 점, 그리고 그 원인을 프롬프트의 **규칙 적용 순서**까지
좁혀 들어간 점이 좋았습니다.

**한 줄 코멘트 — 하나만 짚자면**: `REPORT.md` 가 `experiment_log.csv` 보다 **한 회차 뒤처져**
있습니다. 리포트에는 범위밖 인식률이 0.550 으로 적혀 있고 §4.3 끝에 "트랩 우선순위 규칙을
추가하는 방향을 **다음 개선 과제로 남겨둡니다**" 라고 되어 있는데, 실험 기록에는 그 개선을
**이미 하셨고 효과도 나온 것**으로 남아 있습니다.

```csv
# experiment_log.csv
회차 0  초기 상태(베이스라인)        정확도 0.883 · F1 0.906 · 범위밖 0.55  · 답변 1.0
회차 1  prompts.py (ROUTE_GUIDE)   정확도 0.883 · F1 0.912 · 범위밖 0.75  · 답변 1.0
        "판단 원칙 0번 신설 — 키워드 매칭 전에 개인정보조회·미래예측·서류요청·
         타기관상담·미보유문서 5개 트랩 유형부터 확인"
        "범위밖 오분류 9건→5건으로 감소"
```

**범위밖 0.55 → 0.75 는 이 프로젝트에서 가장 큰 개선인데 리포트에 안 올라가 있습니다.**
§4.1 표와 §4.3 마지막 문단만 고치면 됩니다. 손해 보고 계신 부분이라 꼭 짚어 드리고 싶었습니다.

---

### 4. 견고성 (Robustness)

*실패하는 경로가 설계되어 있는가*

- [O] 에러·예외 처리가 눈에 띈다 (도구 호출 실패, 빈 결과 등)
- [O] 폭주 방지 장치가 있다

**근거 1 — 상한이 한곳에 모여 있고 주석으로 뜻이 적혀 있음**

```python
# config.py
CONF_THRESHOLD = 0.5          # 라우팅 확신도 임계값
MAX_TOOL_TURNS = 3            # 도구 호출 루프 상한
GUARDRAIL_RETRY = 1           # 가드레일 위반 시 재생성 횟수
WORKERS = 8                   # 동시 호출 수. 요청 한도에 걸리면 낮춘다
GRADIO_PORT = 7861            # 어제 실습(모두몰)의 기본 포트 7860과 겹치지 않게 분리
```

도구 루프를 `recursion_limit` 같은 프레임워크 옵션이 아니라 **`for _ in range(MAX_TOOL_TURNS)`**
로 직접 막은 것(`answer.py:55`)이 오히려 확실하다고 봤습니다. 프레임워크가 바뀌어도 안 깨집니다.

**근거 2 — 요청 한도(rate limit) 백오프를 직접 구현**

```python
# common.py
def _retry_delay(e, attempt, base_delay, max_delay): ...
def call_with_retry(fn, *args, max_retries=5, base_delay=5, max_delay=60, **kwargs):
    for attempt in range(max_retries + 1):
        try: ...
        except Exception as e:
            if attempt == max_retries or not _is_rate_limit_error(e): raise
```

특히 이 부분이 인상 깊었습니다 — 서버가 내려주는 안내 문구에서 대기 시간을 **읽어 냅니다.**

```python
"""'retry in 5.2s', 'try again in 2m46.752s' 같은 안내를 초 단위로 읽는다."""
```

고정 지연으로 때우지 않고 서버가 알려 준 값을 쓰는 것은 저는 생각 못 했던 처리입니다.
모델 호출에도 `timeout=60, max_retries=2` 가 따로 걸려 있어 이중으로 막혀 있습니다.

**근거 3 — 병렬 처리가 실제로 되는지까지 측정** (§4.5)

> 동일한 LLM 호출 8건을 `workers=1`(직렬)과 `workers=8`(병렬)로 각각 실행한 결과,
> 13.71초 → 1.23초로 약 11배 단축을 확인했습니다.

"만들었다" 가 아니라 "재 봤다" 로 적은 항목이라 신뢰가 갔습니다.

- **(보너스) 이상한 입력을 하나 넣어봤다** → LLM 호출은 코더님 키가 필요해 직접 돌리지는
  않았고, 대신 `data/hard_cases.csv` 30건과 §4.3 의 오분류 8건을 읽었습니다.
  "자퇴하면 나중에 법적으로 문제가 생기나요?"(법률 자문), "제 학번으로 확인해주실 수
  있나요?"(개인 조회) 같은 **함정을 직접 설계해 넣어 두신 점**이 좋았습니다.
  이상한 입력을 리뷰어가 만들 필요가 없게 미리 넣어 두신 셈입니다.

---

### 5. 작업 추적성 (Traceability)

*AI와 함께 만든 과정이 남아 있는가*

- [△] 작업 흔적이 남아 있다 → **`experiment_log.csv` 는 훌륭하지만 커밋은 1개**
- [O] 코더가 **고친 부분**이 무엇인지 알 수 있다

**근거 1 — 실험 기록이 매우 충실함**

`experiment_log.csv` 의 열 구성이 그 자체로 방법론입니다.

```
round · timestamp · change_target · change_reason · change_detail
      · router_acc · router_macro_f1 · outscope_recall · answer_pass_rate · memo
```

**무엇을 왜 바꿨고 네 지표가 각각 어떻게 움직였는지**가 한 줄에 다 있습니다.
특히 `memo` 에 부작용까지 적어 두신 것:

> 부작용: 자퇴 등록금 환급·시험 부정행위 질문 2건이 트랩 규칙에 걸려 새로 OTHER 오분류
> → 4-카테고리 정확도 개선분 상쇄

**근거 2 — 다만 git 이력은 1개 커밋**

```
$ git log --oneline
c4cc4be Trim history-of-fix phrasing from REPORT.md, update remaining-tasks note
총 1개 커밋
```

`experiment_log.csv` 덕분에 **무엇을 고쳤는지는 추적됩니다.** 다만 커밋이 하나라
"회차 1 직전 코드" 로 되돌려 재현해 보는 것은 어렵습니다.
회차마다 커밋을 하나씩 남기셨다면 `git diff` 로 프롬프트 변경분을 바로 볼 수 있었을 것 같습니다.
(제 저장소도 처음엔 같은 문제였고, 회차마다 커밋하기 시작한 뒤에 되돌려 보기가 쉬워졌습니다.)

**❓ 코더에게 질문 1개**

> **멀티턴에서 "직전 턴을 이어붙이지 않고 현재 질문만으로 독립 판단" 하기로 정하신 이유가
> 궁금합니다.** §5 에 "이전 주제가 새 질문에 섞이지 않는다" 는 이점이 적혀 있는데,
> 그 대신 "장학금 성적 기준이 뭔가요?" → **"그건 언제까지 신청해요?"** 같은 되묻기는
> 주어가 없어서 분류가 어려워질 것 같았습니다.
>
> 데모에서 이런 되묻기를 해 보셨을 때 어떻게 나왔는지, 그리고 학사 상담에서는
> **주제 섞임**과 **되묻기 실패** 중 어느 쪽이 더 위험하다고 보셨는지 듣고 싶습니다.
> (저는 반대로 이어붙이는 쪽을 골랐다가 주제 섞임을 겪었고, 지금은 "지시어가 있을 때만
> 이어붙이는" 절충으로 가 있는데 이것도 정답인지 모르겠습니다.)

---

## 리뷰어 총평 (3줄)

```
- 좋았던 점 1가지:
  지표가 내려간 회차를 지우지 않고, 오분류 8건을 표로 나열한 뒤 원인을 "프롬프트 규칙의
  적용 순서"까지 좁혀 들어간 §4.3. 점수를 자랑하는 리포트가 아니라 "무엇을 왜 틀렸는지"
  를 남긴 리포트라서, 읽는 사람이 다음에 뭘 해야 할지 알 수 있었습니다.

- 딱 하나만 바꾼다면:
  REPORT.md §4.1 과 §4.3 끝부분을 experiment_log.csv 의 회차 1 로 맞춰 주세요.
  범위밖 인식률 0.55 → 0.75 개선이 이미 되어 있는데 리포트에만 안 올라가 있어서,
  지금 리포트를 읽으면 실제보다 낮게 보입니다. 손해 보고 계십니다.

- 내 프로젝트에 가져가고 싶은 것:
  (1) common.py 의 rate limit 백오프 — 서버가 준 "retry in 5.2s" 를 파싱해서 그만큼만
      기다리는 처리. 저는 고정 재시도만 걸어 뒀는데 이쪽이 훨씬 정확합니다.
  (2) experiment_log.csv 의 열 구성 — 변경 대상·이유·상세 + 네 지표를 한 줄에 두는 형식.
      저는 개선 기록을 리포트 본문 표로만 갖고 있어서 기계로 다시 읽을 수가 없었습니다.
```

---

### 덧붙이는 관찰 하나 (참고만 해 주세요)

**1턴 답변 통과율 100% (16/16)** 가 여러 회차에 걸쳐 고정으로 재현된다는 점이 눈에 띄었습니다.
저도 제 프로젝트에서 분류 정확도가 100% 로 나온 적이 있는데, 알고 보니 **잘해서가 아니라
평가셋이 그 지표를 시험하지 못해서**였습니다. 경계에 걸치는 문항을 따로 만들어 넣으니
100% → 89.6% 로 떨어졌고, 그제서야 고칠 자리가 보였습니다.

골든셋 16건에 이미 함정이 충분히 들어 있다면 그대로 두셔도 될 것 같고, 혹시 여유가 되시면
`hard_cases.csv` 30건 중 답변 단계를 흔들 만한 것(예: 도구가 준 분수를 퍼센트로 바꿔야 하는
문항 — §4.4 에서 짚으신 가드레일 과탐 구간)을 골든셋에 넣어 보시면 어떨까 합니다.
100% 가 내려가면 그게 곧 다음에 고칠 지점이 될 것 같습니다.
